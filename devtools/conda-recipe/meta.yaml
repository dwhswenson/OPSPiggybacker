package:
    name: ops_piggybacker-dev
    version: "0.1.0"

source:
    path: ../../

requirements:
    build:
        - python
        - setuptools
        - pyyaml

    run:
        - python
        - numpy
        - scipy
        - pandas
        - openpathsampling

test:
    imports:
        - ops_piggybacker
